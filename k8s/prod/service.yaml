apiVersion: v1
kind: Service
metadata:
  name: tfg-uoc-app
  namespace: prod
spec:
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8888
  selector:
    run: tfg-uoc-app
  loadBalancerSourceRanges:
  - 83.32.0.0/12
